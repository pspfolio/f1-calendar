import React from 'react';

const Info = React.createClass({
    render() {
        return(
            <div>
                <h2>This is info</h2>
            </div>
        )
    }
});

export default Info;
